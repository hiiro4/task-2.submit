<div class="e-box">
<h1>Editing Book</h1>

<% if @book.errors.any? %>
 <%= @book.errors.count %>error prohibited this book from being saved:
<ul>
  <% @book.errors.full_messages.each do |message| %>
  <li><%=message %></li>
<% end %>
</ul>
<% end %>

<%=form_with model:@book,url:update_book_path(@book.id),method: :patch do |f| %>

 <p class="e-text">Title</p>

<%=f.text_field:title %>

<p class="e-text">Body</p>
<%=f.text_area:body %>

<div class="e-submit"><%=f.submit"Update Book" %></div>

<%=link_to"Show",book_path(@book),class: "e-a"%>
|
<%=link_to"Back","/books",class: "e-a"%>
</div>
<% end %>